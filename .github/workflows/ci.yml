name: CI

on:
    pull_request:
        branches: [main]
    push:
        branches: [main]

jobs:
    qa:
        runs-on: ubuntu-latest

        services:
            database:
                image: postgres:16-alpine
                env:
                    POSTGRES_DB: app
                    POSTGRES_USER: app
                    POSTGRES_PASSWORD: '!ChangeMe!'
                ports:
                    - 5432:5432
                options: >-
                    --health-cmd "pg_isready -d app -U app"
                    --health-interval 10s
                    --health-timeout 5s
                    --health-retries 5

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Setup PHP
              uses: shivammathur/setup-php@v2
              with:
                  php-version: '8.4'
                  extensions: ctype, iconv, gd, intl, pdo_pgsql
                  coverage: none
                  tools: composer:v2

            - name: Get Composer cache directory
              id: composer-cache
              run: echo "dir=$(composer config cache-files-dir)" >> "$GITHUB_OUTPUT"

            - name: Cache Composer packages
              uses: actions/cache@v4
              with:
                  path: ${{ steps.composer-cache.outputs.dir }}
                  key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
                  restore-keys: ${{ runner.os }}-composer-

            - name: Install PHP dependencies
              run: composer install --no-interaction --prefer-dist --no-progress

            - name: Composer audit
              run: vendor/bin/castor composer-audit --no-interaction

            - name: Setup Node
              uses: actions/setup-node@v4
              with:
                  node-version: '22'
                  cache: npm

            - name: Install Node dependencies
              run: npm ci

            - name: Build Tailwind CSS
              run: vendor/bin/castor tailwind-build --no-interaction

            - name: Create test database
              run: vendor/bin/castor create-db --test --no-interaction

            - name: Run migrations
              run: vendor/bin/castor migrate --test --no-interaction

            - name: Load fixtures
              run: vendor/bin/castor load-fixtures --test --no-interaction

            - name: PHPStan
              run: vendor/bin/castor phpstan --no-interaction

            - name: ECS
              run: vendor/bin/castor ecs --no-interaction

            - name: TwigCS
              run: vendor/bin/castor twigcs --no-interaction

            - name: PHPMND
              run: vendor/bin/castor phpmnd --no-interaction

            - name: ESLint
              run: vendor/bin/castor eslint --no-interaction

            - name: Schema validate
              run: vendor/bin/castor schema-validate --test --no-interaction

            - name: PHPUnit
              run: vendor/bin/castor test --no-interaction

            - name: Vitest
              run: vendor/bin/castor test-js --no-interaction

    deploy:
        needs: qa
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        runs-on: ubuntu-latest

        steps:
            - name: Checkout
              uses: actions/checkout@v4
              with:
                  fetch-depth: 0

            - name: Deploy to Scalingo
              run: |
                  mkdir -p ~/.ssh
                  echo "$SCALINGO_SSH_KEY" > ~/.ssh/id_ed25519
                  chmod 600 ~/.ssh/id_ed25519
                  ssh-keyscan -H ssh.osc-fr1.scalingo.com >> ~/.ssh/known_hosts
                  git push git@ssh.osc-fr1.scalingo.com:enurus.git HEAD:main
              env:
                  SCALINGO_SSH_KEY: ${{ secrets.SCALINGO_SSH_KEY }}
