<svg class="w-{{ width|default(3.5) }} h-{{ height|default(3.5) }}{% if extraClass|default(false) %} {{ extraClass }}{% endif %}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="{{ strokeWidth|default(2) }}"{% if ariaHidden|default(false) %} aria-hidden="true"{% endif %}>
    <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"></path>
</svg>
