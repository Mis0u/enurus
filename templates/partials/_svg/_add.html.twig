<svg class="w-{{ width|default(5) }} h-{{ height|default(5) }}{% if extraClass|default(false) %} {{ extraClass }}{% endif %}" viewBox="0 0 24 24" fill="none" stroke="{{ strokeColor|default('currentColor') }}" stroke-width="2.5"{% if ariaHidden|default(false) %} aria-hidden="true"{% endif %}>
    <path stroke-linecap="round" stroke-linejoin="round" d="M12 4.5v15m7.5-7.5h-15"></path>
</svg>
