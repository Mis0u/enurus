<svg class="w-{{ width|default(3.5) }} h-{{ height|default(3.5) }}{% if extraClass|default(false) %} {{ extraClass }}{% endif %}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"{% if ariaHidden|default(false) %} aria-hidden="true"{% endif %}>
    <rect x="5" y="11" width="14" height="10" rx="2"></rect>
    <path stroke-linecap="round" d="M8 11V7a4 4 0 0 1 8 0v4"></path>
</svg>
