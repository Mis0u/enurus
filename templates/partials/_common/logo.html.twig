<div class="text-center mb-4">
    <img src="{{ asset('images/logo.png') }}" alt="logo {{ 'name'|trans({}, 'brand') }}" class="mx-auto h-40 mb-2">
    <h1 class="text-3xl font-bold text-white mb-2">{{ 'name'|trans({}, 'brand') }}</h1>
</div>
