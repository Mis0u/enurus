{% set is_active = app.current_route == this.route %}

{% if this.mobile %}
    {% include 'components/Navigation/Sidebar/_mobile_format.html.twig' with {'is_active': is_active} %}
{% else %}
    {% include 'components/Navigation/Sidebar/_desktop_format.html.twig' with {'is_active': is_active} %}
{% endif %}
