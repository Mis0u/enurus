<button
    class="{{ this.classes }}"
    type="{{ type }}"
    {% if data %}
        {{ data }}
    {% endif %}
>
    {% if icon %}
        {% include this.svg %}
        <span>{{ message }}</span>
    {% else %}
        {{ message }}
    {% endif %}
</button>
