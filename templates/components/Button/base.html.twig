<button
    {% if disabled %}disabled{% endif %}
    {% if class is null %}
        class="{{ this.classes }}{% if extraClass is not null %} {{ extraClass }}{% endif %}"
    {% else %}
        class="{{ class }}"
    {% endif %}
    type="{{ type }}"
    {% if data is not null %}
        {{ data }}
    {% endif %}
>
    {% if icon %}
        <div class="flex items-center justify-center space-x-2">
            {% include this.svg only %}
            <span>{{ message }}</span>
        </div>
    {% else %}
        {{ message }}
    {% endif %}
</button>
