<div class="text-center pt-6 mt-6 border-t border-white/10">
    <p class="text-sm text-slate-400">
        {{ 'registration.have_an_account'|trans({}, 'security') }}
        <a href="{{ path('app_login') }}" class="text-cyan-400 hover:text-cyan-300 font-semibold transition-colors">
            {{ 'login.connect'|trans({}, 'security') }}
        </a>
    </p>
</div>
