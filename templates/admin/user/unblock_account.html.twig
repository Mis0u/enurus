{% extends '@EasyAdmin/page/content.html.twig' %}
{% set fr = enum('App\\Enum\\Translations\\LocaleAllowedEnum').FR.value %}

{% block content_title %}{{ 'admin.user.unblock_account.title'|trans({email: user.email}, 'admin', fr) }}{% endblock %}

{% block main %}
    <p>{{ 'admin.user.unblock_account.confirm'|trans({}, 'admin', fr) }}</p>
    <form method="post">
        <input type="hidden" name="_token" value="{{ csrf_token('user_admin_unblock_account_' ~ user.id) }}">
        <button type="submit" class="btn btn-primary">{{ 'admin.user.unblock_account.button'|trans({}, 'admin', fr) }}</button>
    </form>
{% endblock %}
