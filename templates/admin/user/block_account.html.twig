{% extends '@EasyAdmin/page/content.html.twig' %}

{% block content_title %}{{ 'admin.user.block_account.title'|trans({email: user.email}, 'admin', 'fr') }}{% endblock %}

{% block main %}
    <p>{{ 'admin.user.block_account.confirm'|trans({}, 'admin', 'fr') }}</p>
    <form method="post">
        <input type="hidden" name="_token" value="{{ csrf_token('user_admin_block_account_' ~ user.id) }}">
        <button type="submit" class="btn btn-primary">{{ 'admin.user.block_account.button'|trans({}, 'admin', 'fr') }}</button>
    </form>
{% endblock %}
