{% extends '@EasyAdmin/page/content.html.twig' %}

{% block content_title %}{{ 'admin.user.force_logout.title'|trans({email: user.email}, 'admin', 'fr') }}{% endblock %}

{% block main %}
    <p>{{ 'admin.user.force_logout.confirm'|trans({}, 'admin', 'fr') }}</p>
    <form method="post">
        <input type="hidden" name="_token" value="{{ csrf_token('user_admin_force_logout_' ~ user.id) }}">
        <button type="submit" class="btn btn-primary">{{ 'admin.user.force_logout.button'|trans({}, 'admin', 'fr') }}</button>
    </form>
{% endblock %}
