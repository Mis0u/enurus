<div class="flex flex-col gap-4 max-md:gap-3">
    {% for item in exerciseData %}
        {% include 'workout/show/_exercise_card.html.twig' with {
            'item': item,
            'position': loop.index,
            'unit': unit,
        } only %}
    {% endfor %}
</div>
