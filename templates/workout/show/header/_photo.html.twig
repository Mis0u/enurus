<div class="mt-5 pt-5 border-t border-white/[0.07]">
    <div class="flex items-center gap-2 mb-3">
        {% include 'partials/_svg/_picture.html.twig' %}
        <span class="font-['Rajdhani'] text-[18px] font-bold text-[var(--text-primary)] tracking-wider">
            {{ 'workout.photo.label_visible'|trans({}, 'navigation') }}
        </span>
    </div>
    {% include 'workout/show/header/_photo_thumbnail.html.twig' %}
</div>
