# yaml-language-server: $schema=../vendor/symfony/routing/Loader/schema/routing.schema.json

# This file is the entry point to configure the routes of your app.
# Methods with the #[Route] attribute are automatically imported.
# See also https://symfony.com/doc/current/routing.html

# To list all registered routes, run the following command:
#   bin/console debug:router

controllers:
    resource:
        path: ../src/Controller/
        namespace: App\Controller
        exclude: ../src/Controller/Admin/
    type: attribute
    prefix: /{_locale}
    requirements:
        _locale: fr|en|it|es|pt|de|nl|pl
    defaults:
        _locale: en

# EasyAdmin : jamais préfixé par la locale, l'outil n'est vu que par le propriétaire du site
# (dashboard admin en français uniquement, cf. CLAUDE.md).
admin_dashboard:
    resource:
        path: ../src/Controller/Admin/
        namespace: App\Controller\Admin
    type: attribute
