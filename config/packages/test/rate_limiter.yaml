framework:
    rate_limiter:
        registration:
            policy: 'sliding_window'
            limit: 3
            interval: '15 minutes'
            cache_pool: rate_limiter.storage
        password_change:
            policy: 'sliding_window'
            limit: 5
            interval: '15 minutes'
            cache_pool: rate_limiter.storage
        contact:
            policy: 'sliding_window'
            limit: 5
            interval: '15 minutes'
            cache_pool: rate_limiter.storage
