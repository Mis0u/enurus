framework:
    rate_limiter:
        registration:
            policy: 'sliding_window'
            limit: 3
            interval: '15 minutes'
        password_change:
            policy: 'sliding_window'
            limit: 5
            interval: '15 minutes'
        contact:
            policy: 'sliding_window'
            limit: 5
            interval: '15 minutes'
