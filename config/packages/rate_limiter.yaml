framework:
    rate_limiter:
        registration:
            policy: 'sliding_window'
            limit: 3
            interval: '15 minutes'
        password_change:
            policy: 'sliding_window'
            limit: 5
            interval: '15 minutes'
        contact:
            policy: 'sliding_window'
            limit: 5
            interval: '15 minutes'
        email_confirmation_resend:
            policy: 'sliding_window'
            limit: 3
            interval: '15 minutes'
        profile_search:
            policy: 'sliding_window'
            limit: 10
            interval: '15 minutes'
        profile_connection_request:
            policy: 'sliding_window'
            limit: 5
            interval: '1 hour'
